# Logs

Raw execution logs are intentionally not included in this public repository because they may contain local paths, account identifiers, environment details, or credentials.

The experiment Markdown files record the relevant observations and outcomes in sanitized form.
